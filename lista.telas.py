import tkinter as tk

janela = tk.Tk()
janela.title("Janela")
janela.geometry("600x700")
janela.mainloop()


'''def create_buttons(self):
    # Calculate button
    self.calc_button = tk.Button(
        self.window,
        text="Calcular",
        font=self.font,
        command=self.calculate,
        bg="#007acc",
        fg="white",
        width=15
    )
    self.calc_button.grid(row=3, column=0, columnspan=0 , pady=20)

    # Save button
    self.save_button = tk.Button(
        self.window,
        text="Salvar",
        font=self.font,
        command=self.save_to_file,
        bg="#28a745",
        fg="white",
        width=15
    )
    self.save_button.grid(row=7, column=2, pady=20)'''
